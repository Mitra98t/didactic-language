{
  "devDependencies": {
    "@jest/globals": "^29.5.0",
    "@types/jest": "^29.5.1",
    "@types/node": "^20.2.3",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  },
  "scripts": {
    "build": "tsc",
    "exec": "node dist/main.js",
    "start": "npm run build && npm run exec",
    "clean": "rm -rf dist",
    "showOff": "npm run start && echo '\n------\n' && cat filesTest/prg1.lox && echo '\n\n'",
    "test": "jest"
  }
}
