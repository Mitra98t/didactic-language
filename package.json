{
  "devDependencies": {
    "@types/node": "^20.2.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  },
  "scripts": {
    "build": "tsc",
    "exec": "node dist/main.js",
    "start": "npm run build && npm run exec",
    "clean": "rm -rf dist"
  }
}
