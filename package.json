{
  "devDependencies": {
    "@types/node": "^20.2.3",
    "typescript": "^5.0.4"
  },
  "scripts": {
    "build": "tsc",
    "exec": "node dist/main.js",
    "start": "npm run build && npm run exec",
    "clean": "rm -rf dist"
  }
}
